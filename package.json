{
	"private": true,
	"license": "MIT",
	"scripts": {
		"app": "cd app && bun run dev",
		"package": "cd floating-runes && bun run package",
		"publish": "cd floating-runes/npm && bun publish"
	},
	"workspaces": [
		"app",
		"floating-runes"
	],
	"devDependencies": {
		"@changesets/cli": "^2",
		"@svitejs/changesets-changelog-github-compact": "^1",
		"@types/bun": "^1",
		"@types/node": "^22"
	}
}